server:
  port: 8081
spring:
  application:
    name: ch-kafka
  profiles:
    active: local
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.1.2:8848
        namespace: ${nacos.namespace:}
      config:
        server-addr: 192.168.1.2:8848
        namespace: ${nacos.namespace:}
        shared-dataids: ch-kafka.yml #,ch-sentinel.properties
#        shared-dataids: ch-blog.yml